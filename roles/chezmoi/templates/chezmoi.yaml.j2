# vim: ft=yaml

---
encryption: "age"
age:
  identity: "~/.config/chezmoi/key.txt"
  recipient: "age1n6hzpdwerrsnu6wpqf2vmt04fn04tcq9dat9wnh2340h6dd3vqqsev0nue"

# System-specific data
data:
  # what system is running? "laptop" enables special config, but blank is OK too
  system: "laptop"

  # settings for git
  git:
    email: "almond@almendra.dev"
    {% if gpg_signature_git is defined -%}
    gpg:
      enabled: true
      signing_key: {{ gpg_signature_git | tojson }}
    {% else -%}
    gpg:
      enabled: false
    {% endif %}
